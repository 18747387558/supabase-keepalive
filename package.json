{
  "name": "supabase-keepalive",
  "version": "1.0.0",
  "description": "Supabase 免费项目保活脚本",
  "main": "scripts/keep-alive.js",
  "scripts": {
    "build": "tsc scripts/keep-alive.ts --outDir scripts --target es2020 --module commonjs",
    "start": "node scripts/keep-alive.js",
    "dev": "ts-node scripts/keep-alive.ts",
    "test": "echo \"测试保活脚本...\" && npm run dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.7",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0"
  },
  "engines": {
    "node": ">=18"
  }
}
